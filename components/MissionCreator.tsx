import { DateTimePicker } from "./DateTimePicker";

export function MissionCreator({ quoteId, companyId, companyName, serviceLabel }: { quoteId?: string; companyId?: string | null; companyName: string; serviceLabel: string }) {
  return <form action="/api/admin/missions" method="post" className="mission-form"><input type="hidden" name="quoteId" value={quoteId ?? ""}/><input type="hidden" name="companyId" value={companyId ?? ""}/><div className="form-grid two"><label>Titre<input name="title" required defaultValue={`${serviceLabel} · ${companyName}`} /></label><label>Lieu<input name="location" required placeholder="Adresse / ville" /></label></div><div className="mission-schedule"><div className="mission-schedule-heading"><strong>Créneau de mission</strong><span>Sélectionnez le début et la fin de l’intervention.</span></div><div className="mission-picker-grid"><DateTimePicker label="Début" dateName="startDate" timeName="startTime" /><DateTimePicker label="Fin" dateName="endDate" timeName="endTime" /></div></div><div className="form-grid two"><label>Personnel requis<input name="requiredPersonnel" type="number" min="1" defaultValue="1" /></label></div><input type="hidden" name="serviceLabel" value={serviceLabel}/><label>Instructions<textarea name="instructions" rows={3} placeholder="Accès, consignes, contact sur place…" /></label><button className="btn btn-dark">Ajouter au planning</button></form>;
}
